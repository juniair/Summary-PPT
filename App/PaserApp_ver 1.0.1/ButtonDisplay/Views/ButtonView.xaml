<UserControl x:Class="ButtonDisplay.Views.ButtonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Command="{Binding CreateManCommand}" Margin="10">
            <TextBlock TextAlignment="Center">Create<LineBreak></LineBreak>Man</TextBlock>
        </Button>
        <Button Command="{Binding CreateWomanCommand}" Margin="10" Grid.Column="1">
            <TextBlock TextAlignment="Center">Create<LineBreak></LineBreak>Woman</TextBlock>
        </Button>
        
        <Button Content="Object to JSON" Command="{Binding ObjectToJsonCommand}" Margin="10" Grid.Row="1" Grid.ColumnSpan="2" />
        <Button Content="JSON to XML" Command="{Binding JsonToXmlCommand}" Margin="10" Grid.Row="2" Grid.ColumnSpan="2" />
    </Grid>
</UserControl>
