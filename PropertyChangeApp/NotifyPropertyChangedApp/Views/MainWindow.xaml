<Window x:Class="NotifyPropertyChangedApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" Height="350" Width="525">
    <Grid>
        <StackPanel>
            <Label Content="{Binding ClickName}" 
                   FontSize="25"
                   Height="300" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
            <Button Content="Click" Command="{Binding RunCommand}" />
        </StackPanel>
    </Grid>
</Window>
